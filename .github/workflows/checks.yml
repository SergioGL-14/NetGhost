name: Checks

on:
  push:
    branches: [main]
  pull_request:

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Static and engine tests
        shell: pwsh
        run: ./Tests/NetGhost.Tests.ps1
